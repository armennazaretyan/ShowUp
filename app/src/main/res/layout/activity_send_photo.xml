<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.development.blackbox.showup.Activities.SendPhotoActivity">

    <TextView
        android:id="@+id/txtSendToName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="25dp"
        android:textColor="#000"
        android:text="Some name"
        />

    <ImageView
        android:id="@+id/imgEmpty"
        android:layout_width="192sp"
        android:layout_height="192sp"
        android:layout_marginTop="80dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/ic_empty_male"
        />

    <ImageView
        android:id="@+id/imgToSend"
        android:layout_width="192sp"
        android:layout_height="192sp"
        android:layout_centerHorizontal="true"
        android:visibility="invisible"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center"
        android:padding="0dp"
        android:orientation="horizontal">

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAllCaps="false"
            android:text="Cancel"
            android:onClick="onCancel" />

        <Button
            android:id="@+id/btnMakePhoto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAllCaps="false"
            android:text="Make photo"
            android:onClick="onMakePhoto" />

        <Button
            android:id="@+id/btnSend"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:enabled="false"
            android:textColor="#C0C0C0"
            android:textAllCaps="false"
            android:text="Send"
            android:onClick="onSend" />

    </LinearLayout>

</RelativeLayout>
